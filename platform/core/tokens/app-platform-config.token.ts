import { InjectionToken } from '@angular/core';
import { PlatformConfig } from '../config/platform-config';

export const APP_PLATFORM_CONFIG = new InjectionToken<PlatformConfig>('APP_PLATFORM_CONFIG');
